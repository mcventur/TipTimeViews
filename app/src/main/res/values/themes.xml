<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TipTimeViews" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Asignamos en to-do el theme de forma general estilos para los textInput de cualquier tipo
        (incluido TextInputLayout), los radioButton y los switch -->
        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox</item>
        <item name="radioButtonStyle">@style/Widget.TipTimeViews.CompoundButton.RadioButton</item>
        <item name="switchStyle">@style/Widget.TipTimeViews.CompoundButton.Switch</item>
    </style>

    <style name="Theme.TipTimeViews" parent="Base.Theme.TipTimeViews" />
</resources>